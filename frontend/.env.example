# Public frontend configuration only. Never place backend/API secrets here.
VITE_DATA_MODE=live

# Leave empty when the frontend and FastAPI are served behind the same origin or Vite proxy.
VITE_API_BASE_URL=
VITE_WS_BASE_URL=
